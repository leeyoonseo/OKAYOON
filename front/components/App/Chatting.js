import React from 'react';

const Chatting = () => {
    return (
        <div>
            Chatting
        </div>
    );
};

export default Chatting;



// TODO:
// DB 연결
// - 심심이 API 동시간대(그룹채팅?) 접속자 대화가능?
// -- 그렇다면 guest에 태그를 추가해야할듯, fingerprint를 사용해볼까?